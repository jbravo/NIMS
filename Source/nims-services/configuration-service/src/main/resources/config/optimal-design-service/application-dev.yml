server:
  port: 8085

spring:
  cloud:
    config:
      allowOverride: true
      overrideSystemProperties: false
  devtools:
    remote:
      secret: nims

opentracing:
  jaeger:
    const-sampler:
      decision: true
