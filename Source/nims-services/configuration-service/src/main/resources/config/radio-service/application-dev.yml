server:
  port: 8083

spring:
  cloud:
    config:
      allowOverride: true
      overrideSystemProperties: false
  devtools:
    remote:
      secret: nims

opentracing:
  jaeger:
    const-sampler:
      decision: true
